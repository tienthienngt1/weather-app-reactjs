import styled from "styled-components";

const WrapMap = styled.div`
    border: 1px solid green;
    padding: 10px 0;
`

const Map = () => {
    return (
        <WrapMap>
            
        </WrapMap>
    );
};

export default Map;